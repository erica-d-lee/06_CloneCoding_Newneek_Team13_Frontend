import React, { Fragment } from "react";
import styled from "styled-components";

const Search = () => {
    return (
    <Fragment>

    </Fragment>
    );
};

Search .defaultProps = {
    
};


export default Search  ;
