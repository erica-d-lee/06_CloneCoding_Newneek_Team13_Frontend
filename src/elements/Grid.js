import React, { Fragment } from "react";
import styled from "styled-components";

const Grid = () => {
    return (
    <Fragment>

    </Fragment>
    );
};

Grid.defaultProps = {
    
};


export default Grid;

