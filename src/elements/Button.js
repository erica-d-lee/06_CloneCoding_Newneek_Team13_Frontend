import React, { Fragment } from "react";
import styled from "styled-components";

const Button = () => {
    return (
    <Fragment>

    </Fragment>
    );
};

Button.defaultProps = {
    
};


export default Button;
