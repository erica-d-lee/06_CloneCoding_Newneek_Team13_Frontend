import React, { Fragment } from "react";
import styled from "styled-components";

const Image = () => {
    return (
    <Fragment>

    </Fragment>
    );
};

Image.defaultProps = {
    
};


export default Image;