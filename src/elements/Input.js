import React, { Fragment } from "react";
import styled from "styled-components";

const Input = () => {
    return (
    <Fragment>

    </Fragment>
    );
};

Input.defaultProps = {
    
};


export default Input;
