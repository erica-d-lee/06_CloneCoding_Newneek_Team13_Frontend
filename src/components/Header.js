import React from "react";
import styled from "styled-components";

const Header = () => {
    return (
    <Fragment>

    </Fragment>
    );
};

Header.defaultProps = {
    
};


export default Header;