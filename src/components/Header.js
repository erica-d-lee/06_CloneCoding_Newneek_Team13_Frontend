import React from "react";
import styled from "styled-components";

const Header = () => {
    return (
        <React.Fragment>

        </React.Fragment>
    );
};

Header.defaultProps = {
    
};


export default Header;