import React from "react";
import styled from "styled-components";

const Footer = () => {
    return (
    <Fragment>

    </Fragment>
    );
};

Footer.defaultProps = {
    
};


export default Footer;