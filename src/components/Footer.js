import React from "react";
import styled from "styled-components";

const Footer = () => {
    return (
    <React.Fragment>

    </React.Fragment>
    );
};

Footer.defaultProps = {
    
};


export default Footer;